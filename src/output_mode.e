note
	description: "Object representing output modes."
	date: "$Date$"
	revision: "$Revision$"

once class
	OUTPUT_MODE
create
	ilasm, peexe, pedll, object

feature {NONE} -- Creation

	ilasm once  end
	peexe once  end
	pedll once  end
	object once  end

end
